# Environment for the heat-surrogate take-home.
#
# Setup (no conda needed):
#     python3 -m venv .venv
#     source .venv/bin/activate          # Windows: .venv\Scripts\activate
#     pip install -r requirements.txt
#
# These are the only dependencies needed to generate geometries and run the
# Poisson (steady heat) solver. Pinned to the versions this assignment was
# built and tested against.

numpy>=2.2.0,<2.3.0        # arrays / numerics
scipy==1.15.3       # sparse linear solve (used by scikit-fem)
scikit-fem==12.0.1  # lightweight finite-element assembly + solve
gmsh==4.15.2        # mesh generation (rectangle-with-hole tessellation)
meshio==5.3.5       # mesh / field I/O, incl. .vtp and .vtu export
PyYAML==6.0.3       # config-file parsing
pyvista==0.48.4     # .vtp PolyData export (full-field solution) + visualization
vtk==9.6.2          # backend for pyvista (.vtp writer)
torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126

